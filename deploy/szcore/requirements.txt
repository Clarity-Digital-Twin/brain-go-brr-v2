# NOTE: SzCORE runs inference offline. All dependencies must be installed at build time.
#
# The SzCORE Docker image base (`pytorch/pytorch:2.5.0-cuda12.4-cudnn9-runtime`)
# already includes `torch==2.5.0+cu124`. Do not reinstall torch here.

# PyG CUDA wheels for torch 2.5.0 + cu124
-f https://data.pyg.org/whl/torch-2.5.0+cu124.html

numpy==1.26.4
scipy==1.14.1
mne==1.5.1
pyyaml==6.0.2
pydantic==2.10.6

# Flash Linear Attention core (Gated DeltaNet).
# NOTE: `flash-linear-attention` is installed in the Dockerfile with `--no-deps` because it ships `fla.layers`.
fla-core==0.3.2

# `fla.layers` imports `transformers` at module import time (even though we do not use the HF model stack).
transformers==4.57.3

# NOTE: GPU-compiled deps installed in Dockerfile (requires CUDA toolkit + --no-build-isolation):
# - mamba-ssm==2.2.5
# - causal-conv1d==1.5.2

# PyTorch Geometric stack (pinned to project lock)
torch-scatter==2.1.2+pt25cu124
torch-sparse==0.6.18+pt25cu124
torch-cluster==1.6.3+pt25cu124
torch-spline-conv==1.2.2+pt25cu124
torch-geometric==2.6.1

# Model/runtime deps
einops==0.8.0
