# EXACT requirements for Brain-Go-Brr v2
# Generated: 2025-09-21
# Python: 3.11.13
# CUDA Toolkit: 12.1 (MUST be installed separately)
#
# CRITICAL: These exact versions are required for GPU acceleration
# DO NOT use pip install -U or allow automatic upgrades

# Core ML - EXACT versions required
torch==2.2.2+cu121 --index-url https://download.pytorch.org/whl/cu121
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.3.2

# Deep Learning
einops==0.7.0
tqdm==4.66.1

# GPU Extensions (install AFTER CUDA 12.1 toolkit)
# Use: pip install --no-build-isolation <package>
causal-conv1d==1.5.2
mamba-ssm==2.2.4

# EEG Processing
mne==1.7.1
pyedflib==0.1.38

# Configuration
pydantic==2.5.3
pyyaml==6.0.1
click==8.1.7
rich==13.7.0

# Visualization
matplotlib==3.8.2
seaborn==0.13.0

# Optional: Evaluation
pandas==2.1.4

# Optional: Monitoring
# wandb==0.16.2
# tensorboard==2.15.1

# Development (optional)
pytest==8.0.0
pytest-cov==5.0.0
ruff==0.8.0
mypy==1.13.0

# Installation Instructions:
# 1. Install CUDA Toolkit 12.1
# 2. Set CUDA_HOME=/usr/local/cuda-12.1
# 3. pip install -r requirements-exact.txt
# 4. For GPU extensions:
#    pip install --no-build-isolation causal-conv1d==1.5.2
#    pip install --no-build-isolation mamba-ssm==2.2.4